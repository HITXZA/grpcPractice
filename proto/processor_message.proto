syntax= "proto3";
option go_package="./pb;protocol";//这里已经写了输出位置了  如果在命令行里再写个./pb
//相当于是./pb/pb  所以命令行里直接写一个./就行了 这个./就是我们指定的的/pb路径
message CPU{
    //Brand of the CPU
    string brand = 1 ;
    //name of the CPU Core i7-7850
    string name = 2;
    uint32 number_cores = 3 ;
    uint32 number_threads = 4 ;
    double min_ghz = 5;
    double max_ghz = 6;
}
